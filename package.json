{
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "rm -rf dist .cache && parcel build src/index.html"
  },
  "devDependencies": {
    "autoprefixer": "^9.8.6",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "tailwindcss": "^1.6.2"
  }
}
